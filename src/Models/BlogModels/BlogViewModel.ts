export type BlogViewModel = {
  /**
   * blog id
   * name of blog
   * blog description
   * blog website url
   * creation Date
   */
  id: string,
  name: string,
  description: string,
  websiteUrl: string,
  createdAt: string
}